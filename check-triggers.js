require('dotenv').config(); const { Client } = require('pg'); const client = new Client({ connectionString: process.env.SUPABASE_DB_URL || process.env.DATABASE_URL }); client.connect().then(() => client.query('SELECT trigger_name, action_statement FROM information_schema.triggers WHERE event_object_table = \\'leads\\'')).then(res => console.log(res.rows)).catch(console.error).finally(() => client.end());